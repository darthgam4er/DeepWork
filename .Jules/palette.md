## 2025-02-17 - [Accessibility Pattern]
**Learning:** Icon-only buttons (using `lucide-react`) frequently miss `aria-label` attributes, relying on visual cues.
**Action:** When working on interactive components, specifically check all icon-only buttons for missing accessible labels.
